$(".primary-table").html("<%= escape_javascript(render partial: "heat_asset_lists/primary_table", locals: { heat_asset_list: @heat_asset_list }) %>");
$("form.edit_heat_source_list input[type=submit]").removeClass("disabled");
$("span.wait").hide();
