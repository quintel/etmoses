%h3 Combine topology with technologies from ETEngine
.help-block
  %p
    Test test test

- if @import.errors.any?
  %ul.errors
    - @import.errors.full_messages.uniq.each do |message|
      %li= message

- if params[:scenario_id]
  Your ETModel scenario is '#{@import.etm_title}' created by #{current_user.email}

= form_for(@import, url: import_testing_grounds_path, method: :post) do |form|
  .form-group
    = form.label :topology_id
    = tooltip_tag('Select a network topology for your local energy solution. To inspect existing topologies you can visit the topology page in the main menu. You can also create a new topology using the \'create new\' submenu of the topology menu item.' )
    = import_topology_select_tag(form)

  .form-group
    = form.label :scenario_id
    = tooltip_tag('This unique identifier is linked to your scaled ETM scenario. The technologies and base-loads from this scaled scenario will be imported into your local energy solution and connected to the end-points of the local network. ')
    .input-group
      .input-group-addon= TestingGround::IMPORT_PROVIDERS.first
      = form.text_field(:scenario_id, placeholder: 'Scenario ID', class: 'form-control', value: @import.scenario_id)

  %div= form.submit 'Continue&hellip;'.html_safe, class: 'btn btn-primary'
